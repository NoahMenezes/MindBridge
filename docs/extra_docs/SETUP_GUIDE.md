# Setup Guide

1. Install Node.js & Python.
2. Clone repo.
3. Run `pnpm install`.
4. Run `cd backend && python -m venv venv && ./venv/bin/pip install -r requirements.txt`.
5. Run `pnpm dev`.
6. Load extension from `build/firefox-mv2-dev` into Firefox about:debugging.
