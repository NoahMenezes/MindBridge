/**
 * @file types/memory.types.ts
 * @owner Person 2A — MCP Protocol Engineer
 *
 * TypeScript types derived from Zod schemas.
 * Import types from here — never re-declare them manually.
 */

export type {
  MemoryType,
  Memory,
  MemoryResult,
} from "../schemas/memory.schema"
